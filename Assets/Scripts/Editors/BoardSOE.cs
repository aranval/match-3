using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(BoardSO), true)]
public class BoardSOE : Editor
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
