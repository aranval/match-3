using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(BallSO), true)]
public class BallSOE : Editor
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
